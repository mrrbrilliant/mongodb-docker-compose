# MongoDB + Docker Compose

Sample for creating MongoDB container with docker-compose

```sh
# create dotenv file
cp sample.env .env
# edit the dotenv file
# start your database
docker-compose up -d
```
